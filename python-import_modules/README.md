# Python - Import & Modules

This project introduces how to use modules and import statements in Python.
It covers how to:

- Import functions from another file
- Use imported functions
- Create modules in Python
- Use the built-in function `dir()`
- Prevent code from executing when imported (`if __name__ == "__main__"`)
- Handle command-line arguments in Python programs
